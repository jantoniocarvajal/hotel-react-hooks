export interface User {
    id: string;
    userName: string;
    password: string;
    name: string;
}

export interface Hotel {
    id: string;
    name: string;
    address: string;
    phone: string;
    mail: string;
}